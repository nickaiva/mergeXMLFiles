Create directory c:\xml  or use another drive instead of c:
Please add the c:\mergexmlfiles path to system properties such as classpath and path, if necessary  or use another drive instead of c:.
 
Edit the bat file if necessary, to run.

Git commands
git remote add origin git@github.com:nickaiva/MergeXMLFiles.git
git branch -M main
git push -u origin main